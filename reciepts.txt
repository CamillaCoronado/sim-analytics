javascript:(function(){const receipts=[];document.querySelectorAll('div.flex.items-center.px-2').forEach(el=>{const coinEl=el.querySelector('.text-green-500, .text-red-500, [class*="text-green"], [class*="text-red"]');const coins=coinEl?parseInt(coinEl.textContent.replace(/[^\d-]/g,'')):0;const dateEl=el.querySelector('.opacity-60');const date=dateEl?dateEl.textContent.trim():'';const contentEl=el.querySelector('.flex-1.min-w-0');const content=contentEl?contentEl.textContent.trim():'';let user='you';let concept=null;let action='unknown';if(content.includes('You received a tip from')){const tipMatch=content.match(/tip from\s+(.+?)\s+for/);user=tipMatch?tipMatch[1]:'unknown';action='tip';}else if(content.includes('liked your post')){const likeMatch=content.match(/^(.+?)\s+liked/);user=likeMatch?likeMatch[1]:'unknown';action='like';}else if(content.includes('used your concept')){const userMatch=content.match(/^(.+?)\s+used your concept/);user=userMatch?userMatch[1]:'unknown';const conceptMatch=content.match(/concept\s+[^\w\s]*(.+?)\s+to\s+(create|generate)/i);concept=conceptMatch?conceptMatch[1].trim():null;action=conceptMatch?conceptMatch[2]:'use';}receipts.push({user:user.trim(),action,concept,coins,date,raw:content});});navigator.clipboard.writeText(JSON.stringify(receipts)).then(()=>alert(`Copied ${receipts.length} receipts to clipboard! Paste into dashboard.`));})();